name: release
on:
  push:
    tags: ['v*']
permissions:
  contents: write        # create the release and upload assets
  id-token: write        # OIDC token for Sigstore signing (provenance)
  attestations: write    # persist the attestation
jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Compute checksums
        run: sha256sum remove-hidden-devices.ps1 > SHA256SUMS.txt
      - name: Attest build provenance
        uses: actions/attest-build-provenance@v4
        with:
          subject-path: 'remove-hidden-devices.ps1'
      - name: Create release
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh release create "$GITHUB_REF_NAME" --generate-notes remove-hidden-devices.ps1 SHA256SUMS.txt
